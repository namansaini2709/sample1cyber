app.use(express.static(__dirname + '/public'), { setHeaders: (req, res, next) => { res.setHeader('Content-Security-Policy', 'default-src self; script-src self https://example.com; object-src self'); next(); } });